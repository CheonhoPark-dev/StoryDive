# env.example.txt
# 이 파일을 .env 로 복사하여 실제 환경 변수 값을 설정하세요.
# .env 파일은 .gitignore 에 의해 버전 관리에서 제외됩니다.

# Gemini API Key
GEMINI_API_KEY="YOUR_GEMINI_API_KEY_HERE"

# Flask 설정 (필요에 따라 추가)
# FLASK_APP="backend/app.py"
# FLASK_ENV="development" 